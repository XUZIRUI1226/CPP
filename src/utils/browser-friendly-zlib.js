// src/mocks/browser-friendly-zlib.js
export const inflateRawSync = () => {
    // Mock implementation or throw an error if not applicable
    console.log("inflateRawSync is not supported in the browser.");
  };
  
  export const gunzipSync = () => {
    // Mock implementation or throw an error if not applicable
    console.log("gunzipSync is not supported in the browser.");
  };
  